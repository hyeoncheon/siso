<!DOCTYPE html>
<html>
<head>
  <title>SiSO</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="outter-frame">

<div id="page-head">
  <div id="info-box">
  <% if user_signed_in? %>
    <div id="info-user">
      <ul>
        <% if current_user.image == nil %>
        <%= image_tag current_user.gravatar_url %>
        <% else %>
        <img src="<%= url_for(:controller => "users", :action => "photo",
                              :id => session[:user]) %>" />
        <% end %>
        <span class="ic-inline ic-user"></span>
        <span><%= session[:name] %></span>
        <span><%= session[:mail] %></span>
        <li><%= link_to "settings", services_path %></li>
        <li><%= link_to "signout", signout_services_path %></li>
      </ul>
    </div>
  <% end %>
  </div>
  <h1 id="prod-title">SiSO</h1>
  <span id="prod-comment">Single Sign On service for Hyeoncheon Family.</span>
</div>

<div id="flash-wrapper">
<% flash.each do |key, value| %>
  <div class="flash <%= key %> round"><%= value %></div>
<% end %>
</div>

<div id="page-body">
<% if user_signed_in? %>

  <%= yield %>

<% else %>
  <div id="login-dialog" class="siso-dialog">
    <h2 class="siso-dialog-head">Login</h2>
    <div class="siso-dialog-body">
      <span>Signin with</span>
      <%= link_to "LDAP", "/auth/ldap" %>,
      <%= link_to "OpenID", "/auth/open_id" %>
      or
      <%= link_to "Other", signin_services_path %>
    </div>
  </div>
<% end %>
</div>
<div id="page-tail-placeholder"></div>
</div>
<!-- page-tail placed outside of frame -->
<div id="page-tail">
  <ul>
    <li>SiSO - Member of Hyeoncheon, the Black Sky</li>
    <li>powered by RubyOnRails, OmniAuth, Doorkeeper and Scinix</li>
  </ul>
</div>

</body>
</html>
